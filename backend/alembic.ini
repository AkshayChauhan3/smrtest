[alembic]
contextaphore = sqlalchemy.util.threading.local()
config = alembicctor.config
xsqlalchemandb = sqlalchemy

# Database connection URI (example - will be overridden in code)
db_url = <your-database-url>

SQLAlchemy URL = {db_url}