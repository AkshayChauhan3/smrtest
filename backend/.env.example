APP_NAME=SmartRail OS
APP_ENV=development
API_V1_PREFIX=/api/v1
CORS_ORIGINS=http://localhost:3000,http://localhost:5173
JWT_SECRET_KEY=change-me
JWT_ALGORITHM=HS256
JWT_ACCESS_TOKEN_EXPIRE_MINUTES=30

# Development only: pin simulation clock to bypass the 06:20-22:09 service window.
# Comment out (or leave empty) for real-time mode.
# DEV_SIM_TIME=09:00
